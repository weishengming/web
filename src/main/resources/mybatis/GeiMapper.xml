<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weishengming.dao.mapper.GeiMapper">
    <sql id="all_columns">
        ID,CREATEDATE,UPDATEDATE,VERSION,NEIRONG,OPENID,ZHUANGTAI
    </sql>
    <sql id="where" >
        <where>
                	<if test="id != null">
            	and ID = #{id}
            </if>
                	<if test="createDate != null">
            	and CREATEDATE = #{createDate}
            </if>
                	<if test="updateDate != null">
            	and UPDATEDATE = #{updateDate}
            </if>
                	<if test="version != null">
            	and VERSION = #{version}
            </if>
                	<if test="neirong != null">
            	and NEIRONG = #{neirong}
            </if>
                	<if test="openID != null">
            	and OPENID = #{openID}
            </if>
                	<if test="zhuangtai != null">
            	and ZHUANGTAI = #{zhuangtai}
            </if>
                </where>
    </sql>
    <sql id="orderById"> ORDER BY ID DESC </sql>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="GeiDO">
        INSERT INTO gei (<include refid="all_columns"/>
        ) VALUES 
        (#{id},#{createDate},#{updateDate},#{version},#{neirong},#{openID},#{zhuangtai})
    </insert>
    <update id="update" parameterType="GeiDO">
        UPDATE gei SET
                    <![CDATA[ ID = #{id}]]>,   
                    <![CDATA[ CREATEDATE = #{createDate}]]>,   
                    <![CDATA[ UPDATEDATE = #{updateDate}]]>,   
                    <![CDATA[ VERSION = #{version}]]>,   
                    <![CDATA[ NEIRONG = #{neirong}]]>,   
                    <![CDATA[ OPENID = #{openID}]]>,   
                    <![CDATA[ ZHUANGTAI = #{zhuangtai}]]>,   
                version = version+1 
        WHERE
        id=#{id} and version = #{version}
    </update>
    <select id="findOne" parameterType="long" resultType="GeiDO">
        SELECT <include refid="all_columns"/>
        FROM gei WHERE ID=#{id}
    </select>
    <select id="findAll" resultType="GeiDO">
        SELECT <include refid="all_columns"/>
        FROM gei
    </select>
    <delete id="delete" parameterType="long">
        DELETE FROM gei WHERE id=#{id}
    </delete>
    <delete id="deleteByIds">
        DELETE FROM gei
        <where>
            ID in
            <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </delete>
    
    <select id="findList" resultType="GeiDO">
        SELECT <include refid="all_columns"/>
        FROM gei
        <include refid="where"/> <include refid="orderById"/>
        LIMIT #{offset}, #{size}
    </select>
    <select id="getByIds" parameterType="list" resultType="GeiDO">
        SELECT <include refid="all_columns"/>
        FROM gei
        <where>
            ID in
            <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </select>
    <select id="countList" resultType="long">
        SELECT count(1) FROM gei <include refid="where"/> <include refid="orderById"/>
    </select>
    <select id="count" resultType="long">
        SELECT count(1) FROM gei  <include refid="orderById"/>
    </select>
</mapper>
