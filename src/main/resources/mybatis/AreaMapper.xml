<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weishengming.dao.mapper.AreaMapper">
    <sql id="all_columns">
        ID,CREATEDATE,UPDATEDATE,VERSION,CODE,LEVEL,NAME,PARENTID,STATUS,ZIPCODE
    </sql>
    <sql id="where" >
        <where>
                	<if test="id != null">
            	and ID = #{id}
            </if>
                	<if test="createDate != null">
            	and CREATEDATE = #{createDate}
            </if>
                	<if test="updateDate != null">
            	and UPDATEDATE = #{updateDate}
            </if>
                	<if test="version != null">
            	and VERSION = #{version}
            </if>
                	<if test="code != null">
            	and CODE = #{code}
            </if>
                	<if test="level != null">
            	and LEVEL = #{level}
            </if>
                	<if test="name != null">
            	and NAME = #{name}
            </if>
                	<if test="parentId != null">
            	and PARENTID = #{parentId}
            </if>
                	<if test="status != null">
            	and STATUS = #{status}
            </if>
                	<if test="zipCode != null">
            	and ZIPCODE = #{zipCode}
            </if>
                </where>
    </sql>
    <sql id="orderById"> ORDER BY ID DESC </sql>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="AreaDO">
        INSERT INTO area (<include refid="all_columns"/>
        ) VALUES 
        (#{id},#{createDate},#{updateDate},#{version},#{code},#{level},#{name},#{parentId},#{status},#{zipCode})
    </insert>
    <update id="update" parameterType="AreaDO">
        UPDATE area SET
                    <![CDATA[ ID = #{id}]]>,   
                    <![CDATA[ CREATEDATE = #{createDate}]]>,   
                    <![CDATA[ UPDATEDATE = #{updateDate}]]>,   
                    <![CDATA[ VERSION = #{version}]]>,   
                    <![CDATA[ CODE = #{code}]]>,   
                    <![CDATA[ LEVEL = #{level}]]>,   
                    <![CDATA[ NAME = #{name}]]>,   
                    <![CDATA[ PARENTID = #{parentId}]]>,   
                    <![CDATA[ STATUS = #{status}]]>,   
                    <![CDATA[ ZIPCODE = #{zipCode}]]>,   
                version = version+1 
        WHERE
        id=#{id} and version = #{version}
    </update>
    <select id="findOne" parameterType="long" resultType="AreaDO">
        SELECT <include refid="all_columns"/>
        FROM area WHERE ID=#{id}
    </select>
    <select id="findAll" resultType="AreaDO">
        SELECT <include refid="all_columns"/>
        FROM area
    </select>
    <delete id="delete" parameterType="long">
        DELETE FROM area WHERE id=#{id}
    </delete>
    <delete id="deleteByIds">
        DELETE FROM area
        <where>
            ID in
            <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </delete>
    
    <select id="findList" resultType="AreaDO">
        SELECT <include refid="all_columns"/>
        FROM area
        <include refid="where"/> <include refid="orderById"/>
        LIMIT #{offset}, #{size}
    </select>
    <select id="getByIds" parameterType="list" resultType="AreaDO">
        SELECT <include refid="all_columns"/>
        FROM area
        <where>
            ID in
            <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </select>
    <select id="countList" resultType="long">
        SELECT count(1) FROM area <include refid="where"/> <include refid="orderById"/>
    </select>
    <select id="count" resultType="long">
        SELECT count(1) FROM area  <include refid="orderById"/>
    </select>
</mapper>
