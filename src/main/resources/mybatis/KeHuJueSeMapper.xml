<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weishengming.dao.mapper.KeHuJueSeMapper">
	<sql id="all_columns">
		ID,CREATEDATE,UPDATEDATE,VERSION,JUESEMINGCHENG,KEHUZHANGHAO
	</sql>
	<sql id="where">
		<where>
			<if test="id != null">
				and ID = #{id}
			</if>
			<if test="createDate != null">
				and CREATEDATE = #{createDate}
			</if>
			<if test="updateDate != null">
				and UPDATEDATE = #{updateDate}
			</if>
			<if test="version != null">
				and VERSION = #{version}
			</if>
			<if test="juesemingcheng != null">
				and JUESEMINGCHENG = #{juesemingcheng}
			</if>
			<if test="kehuzhanghao != null">
				and KEHUZHANGHAO = #{kehuzhanghao}
			</if>
		</where>
	</sql>
	<sql id="orderById"> ORDER BY ID DESC </sql>
	<insert id="insert" useGeneratedKeys="true" keyProperty="id"
		parameterType="KeHuJueSeDO">
		INSERT INTO kehu_juese (
		<include refid="all_columns" />
		) VALUES
		(#{id},#{createDate},#{updateDate},#{version},#{juesemingcheng},#{kehuzhanghao})
	</insert>
	<update id="update" parameterType="KeHuJueSeDO">
		UPDATE kehu_juese SET
                    <![CDATA[ ID = #{id}]]>,   
                    <![CDATA[ CREATEDATE = #{createDate}]]>,   
                    <![CDATA[ UPDATEDATE = #{updateDate}]]>,   
                    <![CDATA[ VERSION = #{version}]]>,   
                    <![CDATA[ JUESEMINGCHENG = #{juesemingcheng}]]>,   
                    <![CDATA[ KEHUZHANGHAO = #{kehuzhanghao}]]>,
		version = version+1 WHERE
		id=#{id} and version = #{version}
	</update>
	<select id="findOne" parameterType="long" resultType="KeHuJueSeDO">
		SELECT
		<include refid="all_columns" />
		FROM kehu_juese WHERE ID=#{id}
	</select>
	<select id="findAll" resultType="KeHuJueSeDO">
		SELECT
		<include refid="all_columns" />
		FROM kehu_juese
	</select>
	<delete id="delete" parameterType="long">
		DELETE FROM kehu_juese WHERE id=#{id}
	</delete>
	<delete id="deleteByIds">
		DELETE FROM kehu_juese
		<where>
			ID in
			<foreach item="item" index="index" collection="list" open="("
				separator="," close=")">
				#{item}
			</foreach>
		</where>
	</delete>

	<select id="findList" resultType="KeHuJueSeDO">
		SELECT
		<include refid="all_columns" />
		FROM kehu_juese
		<include refid="where" />
		<include refid="orderById" />
		LIMIT #{offset}, #{size}
	</select>
	<select id="getByIds" parameterType="list" resultType="KeHuJueSeDO">
		SELECT
		<include refid="all_columns" />
		FROM kehu_juese
		<where>
			ID in
			<foreach item="item" index="index" collection="list" open="("
				separator="," close=")">
				#{item}
			</foreach>
		</where>
	</select>
	<select id="countList" resultType="long">
		SELECT count(1) FROM kehu_juese
		<include refid="where" />
		<include refid="orderById" />
	</select>
	<select id="count" resultType="long">
		SELECT count(1) FROM kehu_juese
		<include refid="orderById" />
	</select>
</mapper>
