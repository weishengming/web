<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weishengming.dao.mapper.ShouCangMapper">
    <sql id="all_columns">
        ID,CREATEDATE,UPDATEDATE,VERSION,LEIXING,OBJID,OPENID,ZHUANGTAI
    </sql>
    <sql id="where" >
        <where>
                	<if test="id != null">
            	and ID = #{id}
            </if>
                	<if test="createDate != null">
            	and CREATEDATE = #{createDate}
            </if>
                	<if test="updateDate != null">
            	and UPDATEDATE = #{updateDate}
            </if>
                	<if test="version != null">
            	and VERSION = #{version}
            </if>
                	<if test="leixing != null">
            	and LEIXING = #{leixing}
            </if>
                	<if test="objid != null">
            	and OBJID = #{objid}
            </if>
                	<if test="openID != null">
            	and OPENID = #{openID}
            </if>
                	<if test="zhuangtai != null">
            	and ZHUANGTAI = #{zhuangtai}
            </if>
                </where>
    </sql>
    <sql id="orderById"> ORDER BY ID DESC </sql>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="ShouCangDO">
        INSERT INTO shouchang (<include refid="all_columns"/>
        ) VALUES 
        (#{id},#{createDate},#{updateDate},#{version},#{leixing},#{objid},#{openID},#{zhuangtai})
    </insert>
    <update id="update" parameterType="ShouCangDO">
        UPDATE shouchang SET
                    <![CDATA[ ID = #{id}]]>,   
                    <![CDATA[ CREATEDATE = #{createDate}]]>,   
                    <![CDATA[ UPDATEDATE = #{updateDate}]]>,   
                    <![CDATA[ VERSION = #{version}]]>,   
                    <![CDATA[ LEIXING = #{leixing}]]>,   
                    <![CDATA[ OBJID = #{objid}]]>,   
                    <![CDATA[ OPENID = #{openID}]]>,   
                    <![CDATA[ ZHUANGTAI = #{zhuangtai}]]>,   
                version = version+1 
        WHERE
        id=#{id} and version = #{version}
    </update>
    <select id="findOne" parameterType="long" resultType="ShouCangDO">
        SELECT <include refid="all_columns"/>
        FROM shouchang WHERE ID=#{id}
    </select>
    <select id="findAll" resultType="ShouCangDO">
        SELECT <include refid="all_columns"/>
        FROM shouchang
    </select>
    <delete id="delete" parameterType="long">
        DELETE FROM shouchang WHERE id=#{id}
    </delete>
    <delete id="deleteByIds">
        DELETE FROM shouchang
        <where>
            ID in
            <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </delete>
    
    <select id="findList" resultType="ShouCangDO">
        SELECT <include refid="all_columns"/>
        FROM shouchang
        <include refid="where"/> <include refid="orderById"/>
        LIMIT #{offset}, #{size}
    </select>
    <select id="getByIds" parameterType="list" resultType="ShouCangDO">
        SELECT <include refid="all_columns"/>
        FROM shouchang
        <where>
            ID in
            <foreach item="item" index="index" collection="list" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </select>
    <select id="countList" resultType="long">
        SELECT count(1) FROM shouchang <include refid="where"/> <include refid="orderById"/>
    </select>
    <select id="count" resultType="long">
        SELECT count(1) FROM shouchang  <include refid="orderById"/>
    </select>
</mapper>
